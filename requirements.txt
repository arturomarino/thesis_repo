# Colab supplies a CUDA-compatible PyTorch build.  Do not install torch or
# CUDA wheels here: pip can replace that build with an incompatible version.
dask[array]>=2024,<2026.1.2
matplotlib>=3.8,<4
netCDF4>=1.6,<2
numpy>=1.24,<2.3
xarray>=2024,<2026
